import React from 'react';
import {
  View, Text, ScrollView, StyleSheet, FlatList,
} from 'react-native';

const DrawerCustom = () => (
  <View>
    <Text>hello</Text>
  </View>
);

export default DrawerCustom;
